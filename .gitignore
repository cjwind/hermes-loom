__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
# Loom runtime state (never commit a user's ledger/backups)
ledger.db
ledger.db-*
*.loomtmp

# stray export artifact (not part of the project)
Hermes Loom.zip
*.zip
